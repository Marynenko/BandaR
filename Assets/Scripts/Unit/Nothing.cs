using UnityEngine;


public class Nothing
{
    public class PathConstructor : MonoBehaviour
    {
        //public List<Tile> GetNearbyTilesOld(Tile tile, Grid grid)
        //{ 

        //    foreach (var direction in Direction)
        //    {
        //        var coordinateX = Convert.ToInt32(tile.Coordinates.x + direction.XOffset);
        //        var coordinateY = Convert.ToInt32(tile.Coordinates.y + direction.YOffset);

        //        if (coordinateX >= 0 && coordinateX < grid.GridSize.x && coordinateY >= 0 && coordinateY < grid.GridSize.y)
        //        {
        //            var neighbor = grid.Tiles[coordinateX, coordinateY];
        //            if (neighbor != null && neighbor != tile)
        //                nearbyTiles.Add(neighbor);
        //        }
        //    }

        //    tile.Neighbors = nearbyTiles;
        //    return nearbyTiles;
        //}
    }
}